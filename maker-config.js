/**
 * ============================================================
 *  maker-config.js
 *  = 제작사(우리)가 미리 정해두는 에이전트의 "기본 설정(틀)"
 * ============================================================
 *  - 이 파일을 고치면 "모든 에이전트"의 기본 동작이 바뀝니다.
 *  - 사용자가 처음 만들 때 정하는 '이름/성격'과는 별개인, 그 위에 깔린 틀입니다.
 *  - 비개발자도 아래 값(따옴표 안 글, 숫자)만 바꾸면 동작이 달라집니다.
 * ============================================================
 */

module.exports = {
  // ── 1) 뿌리 정체성 ────────────────────────────────
  // 모든 에이전트가 공통으로 갖는 기본 성격·규칙. (지금은 mock이라 참고용,
  // P1에서 진짜 두뇌 연결되면 이 글이 에이전트의 핵심 지침이 됩니다.)
  baseIdentity:
    '당신은 사용자 곁에 있는 다정한 AI 친구입니다. 사용자를 기억하고, 따뜻하고 친근하게 대화합니다.',

  // ── 2) 말투 프리셋 ────────────────────────────────
  // 사용자가 고른 '성격(페르소나)'에 특정 단어가 들어있으면 이 말투가 섞입니다.
  tonePresets: {
    여행: '여행 얘기 항상 설레네요! ',
    글쓰기: '오늘도 멋진 글 쓰실 준비 되셨나요? ',
  },

  // ── 3) 기억 규칙 ──────────────────────────────────
  // 에이전트가 "무엇을" 기억할지. description=사람이 읽는 설명, pattern=실제 인식 규칙.
  memoryRules: [
    { label: '이름', description: '"내 이름은 ○○" → 이름을 기억',
      pattern: /내\s*이름은\s+([^\s,.!?야이]+)(?:야|이야|이에요|예요|입니다)?/g },
    { label: '정체성', description: '"나는 ○○야" → 정체성을 기억',
      pattern: /나는\s+(.+?)(?:야|이야|이에요|예요|입니다)(?:\s|$|[.,!?])/g },
    { label: '좋아하는 것', description: '"○○ 좋아해" → 좋아하는 것을 기억',
      pattern: /([^\s]+)(?:을|를)?\s*좋아해/g },
    { label: '싫어하는 것', description: '"○○ 싫어해" → 싫어하는 것을 기억',
      pattern: /([^\s]+)(?:을|를)?\s*싫어해/g },
  ],

  // ── 4) 행동 설정 ──────────────────────────────────
  behavior: {
    memoryMentionChance: 0.6, // 대화 중 기억을 꺼내 언급할 확률 (0=안함 ~ 1=항상)
    maxFactLength: 30,        // 기억 한 조각의 최대 글자 수
  },
};
